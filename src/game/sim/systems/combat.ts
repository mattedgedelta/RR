/**
 * combat.ts — target acquisition within LOS (only non-allied players are valid
 * targets), approach, attack cooldown, `damage = max(1, atk − armor)`, death and
 * building destruction, and the `underAttack` event (quadrant from map center).
 *
 * Stub: implemented in Phase 10 (Combat / AI / win).
 */
import type { World } from '../world'

export function runCombat(_world: World): void {
  // Phase 10: acquisition, attacks, damage, death, underAttack events
}
