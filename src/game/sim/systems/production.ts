/**
 * production.ts — advances building train queues, spawns finished units at a
 * free edge tile (or moves them to the rally point), and gates on population cap
 * with House production-speed modifiers (e.g. Mars Kennel +40%).
 *
 * Stub: implemented in Phase 9 (Build & train).
 */
import type { World } from '../world'

export function runProduction(_world: World): void {
  // Phase 9: tick queues, spawn at edge, rally, pop-cap gating
}
