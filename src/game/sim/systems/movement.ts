/**
 * movement.ts — path following plus lightweight local separation for unit
 * crowding. Advances each moving unit along its `path` at its def speed.
 *
 * Stub: implemented in Phase 8 (Economy / movement).
 */
import type { World } from '../world'

export function runMovement(_world: World): void {
  // Phase 8: follow path waypoints + separation
}
