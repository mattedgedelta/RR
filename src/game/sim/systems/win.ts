/**
 * win.ts — marks a player defeated when its last production building falls, then
 * resolves the match when only one team remains (FFA = one player). Writes
 * `world.outcome` (victory is from the human's point of view).
 *
 * Stub: implemented in Phase 10 (Combat / AI / win).
 */
import type { World } from '../world'

export function runWin(_world: World): void {
  // Phase 10: elimination + last-team-standing → world.outcome
}
