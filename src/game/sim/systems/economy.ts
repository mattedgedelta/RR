/**
 * economy.ts — the Pioneer gather state machine (toNode → gathering → toDropOff
 * → depositing), resource accrual with House gather modifiers, trailing-window
 * rate tracking, idle-worker detection, and population counting.
 *
 * Stub: implemented in Phase 8 (Economy).
 */
import type { World } from '../world'

export function runEconomy(_world: World): void {
  // Phase 8: gather state machine, accrual, rates, idle detection
}
